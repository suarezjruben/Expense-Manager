# Start dbhub MCP server

Run the dbhub MCP server in the terminal with:

```bash
npx @bytebase/dbhub@latest --transport http --port 8080 --dsn "postgres://user:password@localhost:5432/expenses?sslmode=disable"
```

Run this command in an integrated terminal.
